<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Release: Enforces strict HTTPS for all public network traffic -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Local development endpoints permitted for cleartext debugging -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">192.168.1.16</domain>
    </domain-config>

    <!-- Debug overrides: Allows user-installed certs (e.g. Charles / Proxyman / Fiddler) during debugging -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
